/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "images.pexels.com",
            "www.extraavalue.com",
        ]
    }
}

module.exports = nextConfig
